class Solution {
    /**
     * @param {string} s
     * @param {string} t
     * @return {boolean}
     */
    isAnagram(s, t) {
        //let s_array = s.split('');
        //let t_array = t.split('');
        if(s.length != t.length) {
            return false;
        } 

        let s_array = s.split('');
        let t_array = t.split('');
        let s_map = [];
        let t_map = [];

        for (let i = 0; i < s_array.length; i++) {}

        return false;

    }
}
